---
"text-to-trx": minor
---

Add the `LanguagePack` interface with a filled-in English pack (`en`), and `createTypeRegistry` for resolving default and custom transaction types to their keyword lists.
